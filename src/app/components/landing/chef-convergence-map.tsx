monicablanco @Monicas-MacBook - Pro ~ % export PATH = "$PATH:/Applications/Visual Studio Code.app/Contents/Resources/app/bin"

monicablanco @Monicas-MacBook - Pro ~ % cd ~/Desktop/ik2026
npm install mapbox - gl


up to date, audited 327 packages in 790ms

34 packages are looking for funding
  run`npm fund` for details

1 moderate severity vulnerability

To address all issues, run:
  npm audit fix--force

Run `npm audit` for details.
    monicablanco@Monicas - MacBook - Pro ik2026 % >....
if (!mapContainer.current) return;

const map = new mapboxgl.Map({
    container: mapContainer.current,
    style: "mapbox://styles/mapbox/dark-v11",
    center: [-115.1398, 36.1699], // Las Vegas
    zoom: 2,
});

return () => map.remove();
  }, []);

return <div ref={mapContainer} className="w-full h-[600px]" />;
}

zsh: parse error near`}'
monicablanco@Monicas-MacBook-Pro ik2026 % >....                                                                                                                   
        🍳 Chef origins converging on Las Vegas
      </div>
    </div>
  );
}

function createLabel(text: string): HTMLElement {
  const el = document.createElement("div");
  el.className = "text-xs text-white/80 font-medium";
  el.style.cssText =
    "background: rgba(0,0,0,0.6); padding: 2px 6px; border-radius: 4px; white-space: nowrap; pointer-events: none; transform: translateY(-20px);";
  el.textContent = text;
  return el;
}

zsh: parse error near `}'
monicablanco @Monicas-MacBook - Pro ik2026 % code src / app / components / landing / chef - convergence - map.tsx

monicablanco @Monicas-MacBook - Pro ik2026 % open - a "Visual Studio Code" ~/Desktop/ik2026 / src / app / components / landing / chef - convergence - map.tsx

monicablanco @Monicas-MacBook - Pro ik2026 % >....
map.remove();
mapRef.current = null;
    };
  }, []);

return (
    <div className="relative w-full overflow-hidden rounded-2xl border border-white/10">
        <div ref={mapContainer} className="h-[600px] w-full" />
        <div className="pointer-events-none absolute left-4 top-4 rounded-md bg-black/60 px-3 py-2 text-sm text-white">
            Chef origins converging on Las Vegas
        </div>
    </div>
);
}

zsh: parse error near`}'
monicablanco@Monicas-MacBook-Pro ik2026 % 